# Codex Skill Draft

这份文件保留为兼容入口。

当前最新 agent 剪辑 skill 草案见：

- [`./codex-video-editing-skill.md`](./codex-video-editing-skill.md)

当前口径：

```txt
ffmpeg
  -> 主视频轨、音频、基础字幕、最终 MP4

HyperFrames
  -> 复杂字幕动效、标题、贴纸、包装层、agent visual composition
```

不要默认让 HyperFrames 承担完整主视频轨渲染。
